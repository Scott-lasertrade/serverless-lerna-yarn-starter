import approval from './approval/list';
import remove from './delete';
import { TEST } from '@medii/data';

console.log(TEST);

const functions = {
    approval,
    remove,
};

export default functions;
